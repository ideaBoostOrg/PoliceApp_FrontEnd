import React, { FC } from 'react';
import Services from "./Services";

const Dashboard: FC = () => {
    return (
        <div>
        <Services />
        </div>
    );
    };

export default Dashboard;
